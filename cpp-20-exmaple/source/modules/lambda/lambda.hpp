#pragma once

namespace modules::lambda
{

/**
 * @brief Lambda 表达式：捕获方式、mutable、泛型/模板 lambda、constexpr、
 *        IIFE、递归、与算法配合等各种用法。
 *
 * 详细说明见文档页 @ref lambda （docs/pages/lambda.dox）。
 */
void run();

}  // namespace modules::lambda
